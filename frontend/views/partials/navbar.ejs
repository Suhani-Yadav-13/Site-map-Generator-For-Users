<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <!-- Brand link: conditional redirect -->
    <a class="navbar-brand" href="<%= user ? '/dashboard' : '/' %>">SitemapGen</a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if (user) { %>
          <!-- Logged in links -->
          <li class="nav-item">
            <a class="nav-link <% if(currentPage==='dashboard'){ %>active<% } %>" href="/dashboard">Dashboard</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <% if(currentPage==='generate'){ %>active<% } %>" href="/generate">Generate Sitemap</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <% if(currentPage==='history'){ %>active<% } %>" href="/history">History</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Logout</a>
          </li>
        <% } else { %>
          <!-- Logged out links -->
          <li class="nav-item">
            <a class="nav-link <% if(currentPage==='login'){ %>active<% } %>" href="/login">Login</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <% if(currentPage==='signup'){ %>active<% } %>" href="/signup">Sign Up</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
